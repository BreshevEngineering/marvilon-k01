# INSTALL / UPDATE TO K01 MEDTAS v4

Extract directly into:

`D:\BreshevEngineering\marvilon-k01\`

Do not create a nested v4 project directory.

Launch:
`control\command_center\OPEN_K01_COMMAND_CENTER_V4.cmd`

v4:
- fixes the Git parser shown broken in v3;
- auto-discovers master and registered files;
- reads Gate04D-C2 reports directly;
- adds one-click C2 interference QA;
- adds read-only automatic CAD BOM extraction;
- adds C2 DRAFT drawing generation;
- adds one-click AI Handoff ZIP;
- adds full EDR-005 hard-gate matrix, J2 RVM and dependency graph;
- normalizes the stale raw j2_clocking_m3 label to the actual M2.5 feature for future runs.

No stable CAD promotion and no Git commit are performed by installation.
