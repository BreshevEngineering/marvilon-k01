# K01 Automatic BOM Specification v1

stable A001 → SOLIDWORKS API component traversal → PartNo/config/material/custom
properties → merge master-only BUY/consumables → normalize → audit → CSV/XLSX/JSON.

Release blockers: missing material, duplicate PartNo, CAD/master mismatch, retired
leakage, quantity/unit conflict, unexplained delta.

B007 magnet wire remains AR/m until winding is frozen.

BOM is generated; manual BOM edits are not engineering authority.
